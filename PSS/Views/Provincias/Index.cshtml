@model IEnumerable<PSS.Models.Provincias>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Provincia)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Provincia)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdProvincia">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdProvincia">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdProvincia">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
